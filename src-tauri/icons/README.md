# Tauri应用图标

此目录需要包含以下图标文件:
- 32x32.png (32x32像素PNG图标)
- 128x128.png (128x128像素PNG图标)
- 128x128@2x.png (256x256像素PNG图标)
- icon.icns (macOS图标)
- icon.ico (Windows图标)

您可以使用Tauri CLI生成这些图标:
```bash
npm run tauri icon path/to/your/icon.png
```

或者手动将图标文件放置在此目录中。
